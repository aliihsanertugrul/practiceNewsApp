import { newsData } from "./data/news.js"; 

const loadData = () => {
    for (let i = 0; i < newsData.length; i++) {
        newsData[i];
        console.log(newsData[i].title)
        
    }
}
loadData();